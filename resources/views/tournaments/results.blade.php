@extends('layouts.app')
@section('title', __('ui.results').' · '.$tournament->name)
@section('content')
<div class="page-head"><div><h1>{{ $tournament->name }} — {{ __('ui.results') }}</h1><div class="muted">{{ __('ui.live_standings') }}</div></div><span class="badge {{ $tournament->status->value }}">{{ __('ui.tournament_status_labels.'.$tournament->status->value) }}</span></div>
@include('tournaments._tabs')
@includeWhen(request()->routeIs('public.tournaments.*'), 'tournaments._live_refresh')
@if(!request()->routeIs('public.tournaments.*') && (auth()->user()?->isAdmin() ?? false) && $tournament->format === App\Enums\TournamentFormat::RANKING && $tournament->status === App\Enums\TournamentStatus::LIVE)
<section class="card"><h2>{{ __('ui.record_attempts') }}</h2>@foreach($participants as $participant)<form class="inline-form" method="post" action="{{ route('ranking.attempts.store', [$tournament, $participant]) }}">@csrf<div class="field" style="min-width:200px"><label>{{ $participant->team_name }}</label><div class="muted">{{ $participant->rankingAttempts->count() }} {{ __('ui.saved') }}</div></div><div class="field"><label>{{ __('ui.attempt') }}</label><input type="number" name="attempt_number" min="1" max="{{ $tournament->ranking_config['attempts'] ?? 2 }}" required></div><div class="field"><label>{{ __('ui.value') }}</label><input type="number" name="attempt_value" min="0" step="0.000001"></div><div class="field"><label>{{ __('ui.valid') }}</label><input type="hidden" name="is_valid" value="0"><input style="width:auto" type="checkbox" name="is_valid" value="1" checked></div><button class="btn small">{{ __('ui.save') }}</button></form><hr style="border:0;border-top:1px solid var(--line)">@endforeach</section>
@endif
<section class="card"><h2>{{ __('ui.standings') }}</h2>@if($tournament->format === App\Enums\TournamentFormat::ROUND_ROBIN)<p class="muted standings-rule">{{ __('ui.round_robin_standings_rule') }}</p>@endif<div class="table-wrap standings-wrap"><table class="standings-table"><thead><tr><th>{{ __('ui.rank') }}</th><th>{{ __('ui.participant') }}</th>@if($tournament->format === App\Enums\TournamentFormat::RANKING)<th>{{ __('ui.best_value') }}</th>@else<th>{{ __('ui.played') }}</th><th>{{ __('ui.wins') }}</th><th>{{ __('ui.draws') }}</th><th>{{ __('ui.losses') }}</th><th>{{ __('ui.score_for') }}</th><th>{{ __('ui.score_against') }}</th><th>{{ __('ui.difference') }}</th>@endif</tr></thead><tbody>@forelse($standings as $standing)<tr><td><strong>{{ $standing->rank_number ?: '—' }}</strong></td><td>{{ $standing->participant->team_name }}</td>@if($tournament->format === App\Enums\TournamentFormat::RANKING)<td>{{ $standing->best_value !== null ? (float)$standing->best_value : '—' }}</td>@else<td>{{ $standing->played }}</td><td><strong>{{ $standing->wins }}</strong></td><td>{{ $standing->draws }}</td><td>{{ $standing->losses }}</td><td>{{ (float)$standing->score_for }}</td><td>{{ (float)$standing->score_against }}</td><td>{{ (float)$standing->score_difference }}</td>@endif</tr>@empty<tr><td colspan="9" class="empty">{{ __('ui.standings_empty') }}</td></tr>@endforelse</tbody></table></div></section>
@endsection
@push('styles')
<style>
.standings-rule{margin:-8px 0 12px}
@media(max-width:680px){.standings-table th:first-child,.standings-table td:first-child{position:sticky;left:0;z-index:2;width:48px;background:var(--card)}.standings-table th:nth-child(2),.standings-table td:nth-child(2){position:sticky;left:48px;z-index:2;max-width:150px;overflow:hidden;background:var(--card);text-overflow:ellipsis}.standings-table thead th:first-child,.standings-table thead th:nth-child(2){z-index:3;background:var(--card)}.standings-table th:nth-child(2),.standings-table td:nth-child(2){box-shadow:5px 0 7px -7px rgb(0 0 0 / .75)}}
</style>
@endpush
